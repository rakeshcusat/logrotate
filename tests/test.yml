- hosts: all
  sudo: True
  roles:
    - logrotate
    - role: logrotate
      logrotate_configs:
        - name: apache2
          log_file_path: "/var/log/apache2/access.log"
          frequency: "daily"
          size: 1024M
